#!/usr/bin/env bash
set -euo pipefail
# chạy migration cho API (tùy framework)
# ví dụ: docker compose exec api /app/migrate up
echo "Run your DB migrations here."
echo "Example: docker compose exec api /app/migrate up"
